apiVersion: v1
kind: Service
metadata:
  name: {{ include "hellogalaxy.fullname" . }}
  labels:
    {{- include "hellogalaxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hellogalaxy.selectorLabels" . | nindent 4 }}
