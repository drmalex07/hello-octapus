apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellogalaxy.fullname" . }}-test-connection"
  labels:
    {{- include "hellogalaxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hellogalaxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
