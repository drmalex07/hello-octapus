namespace: default

helm:
  releaseName: 'redis-c1'
  
  chart: "redis"
  repo: "https://charts.bitnami.com/bitnami"
  version: "12.1.2"
  
  valuesFiles:
  - values-local.yml
  values:
    master:
      podLabels:
        updatedAt: '20220107'
    slave:
      podLabels:
        updatedAt: '20220107'

